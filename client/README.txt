In the client side.

1) It will read the MAC address of the appropriate interface of the system.
2) Not hardcoded.
3) It will send the MAC address to the server side.
4) If there is a -m argument then it will send the MAC address which has been
passed as an argument in the command line.
